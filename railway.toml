[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m src.main run --config config/railway.yaml --futures-config config/kraken_futures.yaml --dashboard"
healthcheckPath = "/api/status"
restartPolicyType = "always"
